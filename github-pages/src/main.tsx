import React from "react";
import ReactDOM from "react-dom/client";
import { MotionArchive } from "../../app/MotionArchive";
import "../../app/globals.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <MotionArchive />
  </React.StrictMode>,
);
